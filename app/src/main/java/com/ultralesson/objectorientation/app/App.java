/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.ultralesson.objectorientation.app;

import com.ultralesson.objectorientation.list.LinkedList;

import static com.ultralesson.objectorientation.utilities.StringUtils.join;
import static com.ultralesson.objectorientation.utilities.StringUtils.split;
import static com.ultralesson.objectorientation.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
